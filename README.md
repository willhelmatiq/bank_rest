# Системы Управления Банковскими Картами Bank_rest

## 📌 Описание проекта

Backend-приложение для управления банковскими картами и переводами между ними.  
Сервис поддерживает аутентификацию и авторизацию с использованием JWT, разграничение ролей **ADMIN** и **USER**, а также работу с базой данных через миграции.  
Проект предназначен для демонстрации архитектуры REST API, безопасности, работы с БД и тестирования.

---

## 🛠 Стек технологий

-   Java 21
-   Spring Boot 4.0.0
-   Spring Security + JWT
-   Spring Data JPA
-   PostgreSQL
-   Liquibase
-   Docker / Docker Compose
-   Swagger

---

## 🚀 Запуск проекта

### Запуск локально

Для запуска необходимо иметь поднятую **PostgreSQL** и активировать профиль `local`. Для активации нужно передать в **VM options**  
`-Dspring.profiles.active=local`

### Запуск в dev-среде через Docker Compose

Для запуска необходим установленный **Docker** и **Docker Compose**.

```
mvn clean package
docker-compose build
docker-compose up
```

Будут запущены 2 контейнера:

-   **bank_rest_postgres** — PostgreSQL
-   **bank_rest_app** — Spring Boot приложение Примечания Все защищённые эндпоинты требуют JWT-токен

Swagger UI доступен без авторизации

## 🔧 Профили и конфигурация

Приложение можно также поднять локально, для этого нужно создать

```
application-local.yaml
```

и доопределить там все свойства, которые не определены в

```
application.yaml
```

Для запуска в контейнере используется профиль dev, соответственно дополнительная конфигурация будет лежать в

```
application-dev.yaml
```

## 🔐 Аутентификация и роли

В приложении используется **JWT-аутентификация**.

### Роли доступа

#### **ADMIN**

-   создание карт
-   блокировка и активация карт
-   удаление карт (карты переводятся в статус **CLOSED**)
-   управление пользователями
-   просмотр всех карт в системе

#### **USER**

-   просмотр своих карт (кроме карт со статусом **CLOSED**) с пагинацией
-   запрос блокировки карты
-   переводы между своими картами
-   просмотр баланса карты

## 🧪 Swagger UI

Swagger UI доступен по адресу:

`http://<host>:<port>/swagger-ui/index.html` Где:

-   `<host>` — адрес сервера, на котором запущено приложение
-   `<port>` — порт приложения (по умолчанию `8080`)

### Авторизация в Swagger

-   Выполнить запрос: `POST /api/auth/login`
-   Скопировать JWT-токен из ответа
-   Нажать кнопку Authorize и вставить токен

После этого защищённые эндпоинты станут доступны для тестирования

## 📄 Документация API

OpenAPI спецификация хранится в **docs/openapi.yaml**

## 🗄 Работа с базой данных и миграции

Используется **PostgreSQL** и **Liquibase**  
Миграции выполняются автоматически при старте приложения  
Liquibase миграции расположены в: **src/main/resources/db/migration**

## 🧪 Тестирование

В проекте реализованы unit-тесты ключевой бизнес-логики и контроллеров. Для контроллеров тестируется только их функционал, **без Spring Security**.

#### **Запуск тестов:**

```
mvn test
```

#### **Сборка проекта:**

```
mvn clean package
```